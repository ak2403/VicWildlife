import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    headerView: {
        marginBottom: 20
    },
    headerTitle: {
        fontSize: 32,
        color: '#fff',
        fontWeight: 'bold'
    }
})