import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row', 
        width: '100%',
        marginBottom: 30
    },
    titleText: {
        fontSize: 20,
        color: '#fff',
        fontWeight: 'bold',
        marginBottom: 10
    },
    subtitleText: {
        fontSize: 14,
        color: '#fff',
        marginBottom: 10
    }
});

export default styles